services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - JWT_SECRET=secret
      - ALLOWED_ORIGINS=*
      - DB_PATH=/app/data/worktrack.db
    volumes:
      - .:/app
      - ./data:/app/data
